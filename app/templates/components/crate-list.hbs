<ul>
    {{#each crates as |crate|}}
        <li>
            {{#link-to 'crate' crate.id class='name'}}
                <span>{{ crate.name }} ({{ crate.max_version }})</span>
                {{svg-jar "right-arrow"}}
            {{/link-to}}
        </li>
    {{/each}}
</ul>
