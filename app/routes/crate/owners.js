import Route from '@ember/routing/route';

export default Route.extend({
    // model() {
    //     this.controllerFor(this.routeName)
    //         .set('crate', this.modelFor('crate'));
    // }
});
