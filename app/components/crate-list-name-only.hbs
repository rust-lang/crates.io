<FrontPageList @ordered={{true}} as |Item|>
  {{#each @crates as |crate index|}}
    <Item
      @route="crate"
      @model={{crate.id}}
      @title={{crate.name}}
      data-test-crate-link={{index}}
    />
  {{/each}}
</FrontPageList>