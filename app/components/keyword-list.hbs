<FrontPageList as |Item|>
  {{#each @keywords as |keyword|}}
    <Item @route="keyword" @model={{keyword}}>
      {{ keyword.id }} ({{ format-num keyword.crates_cnt }})
    </Item>
  {{/each}}
</FrontPageList>