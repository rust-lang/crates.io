<FrontPageList as |Item|>
  {{#each @keywords as |keyword|}}
    <Item
      @route="keyword"
      @model={{keyword}}
      @title={{keyword.id}}
      @subtitle="{{format-num keyword.crates_cnt}} crates"
    />
  {{/each}}
</FrontPageList>