<FrontPageList as |Item|>
  {{#each @categories as |category|}}
    <Item
      @route="category"
      @model={{category.slug}}
      @title={{category.category}}
      @subtitle={{format-num category.crates_cnt}}
    />
  {{/each}}
</FrontPageList>