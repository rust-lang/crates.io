<FrontPageList as |Item|>
  {{#each @categories as |category|}}
    <Item @route="category" @model={{category.slug}}>
      {{ category.category }} ({{ format-num category.crates_cnt }})
    </Item>
  {{/each}}
</FrontPageList>