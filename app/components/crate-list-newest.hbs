<FrontPageList @ordered={{true}} as |Item|>
  {{#each @crates as |crate index|}}
    <Item @route="crate" @model={{crate.id}} data-test-crate-link={{index}}>
      {{ crate.name }} ({{ crate.newest_version }})
    </Item>
  {{/each}}
</FrontPageList>