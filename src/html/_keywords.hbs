<ul>
    {{#each keywords as |keyword|}}
        <li>
            <a href="/keywords/{{keyword.slug}}" class="name">
                <span>{{keyword.id}} ({{format_num keyword.crates_cnt}})</span>
                <div class="arrow-in-list">
                    {{embed_svg "right-arrow"}}
                </div>
            </a>
        </li>
    {{/each}}
</ul>
